# ============================================================
# ASTRA OS - Production Docker Compose (Root-Level Entrypoint)
# ============================================================

# Usage: docker compose -f docker-compose.prod.yml up -d

include:
  - docker/prod/docker-compose.yml
