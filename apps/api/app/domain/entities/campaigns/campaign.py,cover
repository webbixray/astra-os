> from dataclasses import dataclass, field
> from datetime import date, datetime
> from uuid import UUID, uuid4
  
> from app.domain.common import now
> from app.domain.exceptions.domain_exceptions import ValidationError
  
  
> @dataclass
> class Campaign:
>     id: UUID = field(default_factory=uuid4)
>     organization_id: UUID = field(default_factory=uuid4)
>     name: str = ""
>     description: str | None = None
>     status: str = "draft"
>     budget_amount: float | None = None
>     budget_currency: str = "USD"
>     start_date: date | None = None
>     end_date: date | None = None
>     channels: list[str] = field(default_factory=list)
>     objective: str | None = None
>     created_by: UUID = field(default_factory=uuid4)
>     ai_generated: bool = False
>     metadata: dict = field(default_factory=dict)
>     created_at: datetime = field(default_factory=now)
>     updated_at: datetime = field(default_factory=now)
  
>     VALID_STATUSES = ["draft", "pending_approval", "active", "paused", "completed", "archived"]
>     VALID_CHANNELS = ["email", "social", "ads", "seo", "content"]
>     VALID_OBJECTIVES = ["awareness", "consideration", "conversion"]
  
>     @classmethod
>     def create(
>         cls,
>         organization_id: UUID,
>         name: str,
>         created_by: UUID,
>         description: str | None = None,
>         budget_amount: float | None = None,
>         budget_currency: str = "USD",
>         start_date: date | None = None,
>         end_date: date | None = None,
>         channels: list[str] | None = None,
>         objective: str | None = None,
>     ) -> "Campaign":
>         instance = cls(
>             organization_id=organization_id,
>             name=name,
>             created_by=created_by,
>             description=description,
>             budget_amount=budget_amount,
>             budget_currency=budget_currency,
>             start_date=start_date,
>             end_date=end_date,
>             channels=channels or [],
>             objective=objective,
>         )
>         instance._validate()
>         return instance
  
>     def _validate(self) -> None:
>         if not self.name or not self.name.strip():
>             raise ValidationError("Campaign name is required")
>         if self.channels:
>             invalid = [c for c in self.channels if c not in self.VALID_CHANNELS]
>             if invalid:
>                 raise ValidationError(f"Invalid channels: {invalid}")
>         if self.objective and self.objective not in self.VALID_OBJECTIVES:
!             raise ValidationError(f"Invalid objective: {self.objective}")
>         if self.start_date and self.end_date and self.start_date > self.end_date:
>             raise ValidationError("Start date must be before end date")
>         if self.budget_amount is not None and self.budget_amount <= 0:
!             raise ValidationError("Budget must be positive")
  
>     def transition_to(self, new_status: str) -> None:
>         allowed_transitions = {
>             "draft": ["pending_approval", "archived"],
>             "pending_approval": ["active", "draft", "archived"],
>             "active": ["paused", "completed", "archived"],
>             "paused": ["active", "completed", "archived"],
>             "completed": ["archived"],
>             "archived": [],
>         }
>         allowed = allowed_transitions.get(self.status, [])
>         if new_status not in allowed:
>             raise ValidationError(
>                 f"Cannot transition from '{self.status}' to '{new_status}'. "
>                 f"Allowed transitions: {allowed}"
>             )
>         self.status = new_status
>         self.updated_at = now()
  
>     def update_budget(self, amount: float) -> None:
>         if amount <= 0:
>             raise ValidationError("Budget must be positive")
>         self.budget_amount = amount
>         self.updated_at = now()
