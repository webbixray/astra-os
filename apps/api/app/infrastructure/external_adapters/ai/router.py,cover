> import json
> import logging
> from collections.abc import AsyncIterator
  
> import httpx
  
> from app.application.ports.ai_provider import AIProvider
> from app.config import config
  
> logger = logging.getLogger(__name__)
  
  
> NVIDIA_NIM_CHAT_URL = f"{config.nvidia_nim_base_url}/chat/completions" if config.nvidia_nim_base_url else ""
> OPENAI_CHAT_URL = "https://api.openai.com/v1/chat/completions"
> ANTHROPIC_CHAT_URL = "https://api.anthropic.com/v1/messages"
  
  
> class NvidiaNIMProvider(AIProvider):
>     def __init__(self) -> None:
>         self.api_key = ""
>         self.base_url = config.nvidia_nim_base_url
  
>     async def stream_chat(
>         self,
>         messages: list[dict],
>         model: str | None = None,
>     ) -> AsyncIterator[str]:
>         if not self.base_url:
>             return
>         async with httpx.AsyncClient(timeout=120.0) as client, client.stream(
>             "POST",
>             f"{self.base_url}/chat/completions",
>             headers={
>                 "Authorization": f"Bearer {self.api_key}",
>                 "Content-Type": "application/json",
>             },
>             json={
>                 "model": model or "meta/llama-3.1-70b-instruct",
>                 "messages": messages,
>                 "stream": True,
>             },
>         ) as response:
>             async for line in response.aiter_lines():
>                 if line.startswith("data: "):
>                     data = line[6:]
>                     if data.strip() == "[DONE]":
>                         break
>                     try:
>                         chunk = json.loads(data)
>                         delta = chunk.get("choices", [{}])[0].get("delta", {})
>                         content = delta.get("content", "")
>                         if content:
>                             yield content
>                     except json.JSONDecodeError:
>                         continue
  
>     async def chat(
>         self,
>         messages: list[dict],
>         model: str | None = None,
>     ) -> str:
>         if not self.base_url:
>             return ""
>         async with httpx.AsyncClient(timeout=120.0) as client:
>             response = await client.post(
>                 f"{self.base_url}/chat/completions",
>                 headers={
>                     "Authorization": f"Bearer {self.api_key}",
>                     "Content-Type": "application/json",
>                 },
>                 json={
>                     "model": model or "meta/llama-3.1-70b-instruct",
>                     "messages": messages,
>                     "stream": False,
>                 },
>             )
>             response.raise_for_status()
>             data = response.json()
>             return data["choices"][0]["message"]["content"]
  
  
> class OpenAIProvider(AIProvider):
>     def __init__(self) -> None:
>         self.api_key = config.openai_api_key
  
>     async def stream_chat(
>         self,
>         messages: list[dict],
>         model: str | None = None,
>     ) -> AsyncIterator[str]:
>         if not self.api_key:
>             return
>         async with httpx.AsyncClient(timeout=120.0) as client, client.stream(
>             "POST",
>             OPENAI_CHAT_URL,
>             headers={
>                 "Authorization": f"Bearer {self.api_key}",
>                 "Content-Type": "application/json",
>             },
>             json={
>                 "model": model or "gpt-4o-mini",
>                 "messages": messages,
>                 "stream": True,
>             },
>         ) as response:
>             async for line in response.aiter_lines():
>                 if line.startswith("data: "):
>                     data = line[6:]
>                     if data.strip() == "[DONE]":
>                         break
>                     try:
>                         chunk = json.loads(data)
>                         delta = chunk.get("choices", [{}])[0].get("delta", {})
>                         content = delta.get("content", "")
>                         if content:
>                             yield content
!                     except json.JSONDecodeError:
!                         continue
  
>     async def chat(
>         self,
>         messages: list[dict],
>         model: str | None = None,
>     ) -> str:
>         if not self.api_key:
>             return ""
>         async with httpx.AsyncClient(timeout=120.0) as client:
>             response = await client.post(
>                 OPENAI_CHAT_URL,
>                 headers={
>                     "Authorization": f"Bearer {self.api_key}",
>                     "Content-Type": "application/json",
>                 },
>                 json={
>                     "model": model or "gpt-4o-mini",
>                     "messages": messages,
>                     "stream": False,
>                 },
>             )
>             response.raise_for_status()
>             data = response.json()
>             return data["choices"][0]["message"]["content"]
  
  
> class AIRouter:
>     def __init__(self) -> None:
>         self.providers: list[AIProvider] = []
>         if config.nvidia_nim_base_url:
>             self.providers.append(NvidiaNIMProvider())
>         if config.openai_api_key:
>             self.providers.append(OpenAIProvider())
  
>     async def stream_chat(
>         self,
>         messages: list[dict],
>         model: str | None = None,
>     ) -> AsyncIterator[str]:
>         for provider in self.providers:
>             try:
>                 async for chunk in provider.stream_chat(messages, model=model):
>                     yield chunk
>                 return
>             except Exception as e:
>                 logger.warning("AI provider %s failed: %s", type(provider).__name__, e)
>                 continue
  
>     async def chat(
>         self,
>         messages: list[dict],
>         model: str | None = None,
>     ) -> str:
>         for provider in self.providers:
>             try:
>                 return await provider.chat(messages, model=model)
>             except Exception as e:
>                 logger.warning("AI provider %s failed: %s", type(provider).__name__, e)
>                 continue
>         return "I'm sorry, I'm having trouble connecting to my AI services right now."
