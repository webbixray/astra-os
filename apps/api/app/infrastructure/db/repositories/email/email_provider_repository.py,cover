> from uuid import UUID
  
> from sqlalchemy import select
> from sqlalchemy.ext.asyncio import AsyncSession
  
> from app.domain.entities.email.email_provider import EmailProvider
> from app.infrastructure.db.models.email.email_provider_model import EmailProviderModel
  
  
> class EmailProviderRepository:
>     def __init__(self, session: AsyncSession):
!         self.session = session
  
>     async def save(self, provider: EmailProvider) -> EmailProvider:
!         model = EmailProviderModel.from_domain(provider)
!         merged = await self.session.merge(model)
!         await self.session.flush()
!         return merged.to_domain()
  
>     async def find_by_id(self, provider_id: UUID) -> EmailProvider | None:
!         result = await self.session.execute(
!             select(EmailProviderModel).where(EmailProviderModel.id == provider_id)
!         )
!         model = result.scalar_one_or_none()
!         return model.to_domain() if model is not None else None
  
>     async def find_by_organization(self, org_id: UUID) -> list[EmailProvider]:
!         result = await self.session.execute(
!             select(EmailProviderModel)
!             .where(EmailProviderModel.organization_id == org_id)
!             .order_by(EmailProviderModel.created_at.desc())
!         )
!         return [m.to_domain() for m in result.scalars().all()]
  
>     async def find_active(self, org_id: UUID) -> EmailProvider | None:
!         result = await self.session.execute(
!             select(EmailProviderModel)
!             .where(
!                 EmailProviderModel.organization_id == org_id,
!                 EmailProviderModel.is_active == True,
!             )
!             .limit(1)
!         )
!         model = result.scalar_one_or_none()
!         return model.to_domain() if model is not None else None
  
>     async def delete(self, provider_id: UUID) -> None:
!         result = await self.session.execute(
!             select(EmailProviderModel).where(EmailProviderModel.id == provider_id)
!         )
!         model = result.scalar_one_or_none()
!         if model is not None:
!             await self.session.delete(model)
!             await self.session.flush()
