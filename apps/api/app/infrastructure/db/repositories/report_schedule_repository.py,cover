> from uuid import UUID
  
> from sqlalchemy import select
> from sqlalchemy.ext.asyncio import AsyncSession
  
> from app.domain.entities.reports.report_schedule import ReportSchedule
> from app.infrastructure.db.models.reports.report_schedule_model import ReportScheduleModel
  
  
> class ReportScheduleRepository:
>     def __init__(self, session: AsyncSession):
!         self.session = session
  
>     async def save(self, schedule: ReportSchedule) -> ReportSchedule:
!         model = ReportScheduleModel.from_domain(schedule)
!         merged = await self.session.merge(model)
!         await self.session.flush()
!         return merged.to_domain()
  
>     async def find_by_id(self, schedule_id: UUID) -> ReportSchedule | None:
!         result = await self.session.execute(
!             select(ReportScheduleModel).where(ReportScheduleModel.id == schedule_id)
!         )
!         model = result.scalar_one_or_none()
!         return model.to_domain() if model is not None else None
  
>     async def find_by_organization(self, org_id: UUID) -> list[ReportSchedule]:
!         result = await self.session.execute(
!             select(ReportScheduleModel)
!             .where(ReportScheduleModel.organization_id == org_id)
!             .order_by(ReportScheduleModel.created_at.desc())
!         )
!         return [m.to_domain() for m in result.scalars().all()]
  
>     async def delete(self, schedule_id: UUID) -> None:
!         result = await self.session.execute(
!             select(ReportScheduleModel).where(ReportScheduleModel.id == schedule_id)
!         )
!         model = result.scalar_one_or_none()
!         if model is not None:
!             await self.session.delete(model)
!             await self.session.flush()
