> from uuid import UUID
  
> from sqlalchemy import select
> from sqlalchemy.ext.asyncio import AsyncSession
  
> from app.domain.entities.campaigns.automation import (
>     AudienceSegment,
>     AutomationRule,
>     BidOptimizationRule,
>     BudgetAllocationRule,
>     ContentRecommendation,
> )
> from app.infrastructure.db.models.campaigns.automation import (
>     AudienceSegmentModel,
>     AutomationRuleModel,
>     BidOptimizationRuleModel,
>     BudgetAllocationRuleModel,
>     ContentRecommendationModel,
> )
  
  
> class BudgetAllocationRuleRepository:
>     def __init__(self, session: AsyncSession):
!         self.session = session
  
>     async def save(self, rule: BudgetAllocationRule) -> BudgetAllocationRule:
!         model = BudgetAllocationRuleModel.from_domain(rule)
!         merged = await self.session.merge(model)
!         await self.session.flush()
!         return merged.to_domain()
  
>     async def find_by_id(self, rule_id: UUID) -> BudgetAllocationRule | None:
!         result = await self.session.execute(
!             select(BudgetAllocationRuleModel).where(BudgetAllocationRuleModel.id == rule_id)
!         )
!         model = result.scalar_one_or_none()
!         return model.to_domain() if model is not None else None
  
>     async def find_by_campaign(self, campaign_id: UUID) -> list[BudgetAllocationRule]:
!         result = await self.session.execute(
!             select(BudgetAllocationRuleModel)
!             .where(BudgetAllocationRuleModel.campaign_id == campaign_id)
!             .order_by(BudgetAllocationRuleModel.name)
!         )
!         return [m.to_domain() for m in result.scalars().all()]
  
>     async def find_by_organization(self, org_id: UUID) -> list[BudgetAllocationRule]:
!         result = await self.session.execute(
!             select(BudgetAllocationRuleModel)
!             .where(BudgetAllocationRuleModel.organization_id == org_id)
!             .order_by(BudgetAllocationRuleModel.name)
!         )
!         return [m.to_domain() for m in result.scalars().all()]
  
>     async def delete(self, rule_id: UUID) -> None:
!         model = await self.session.get(BudgetAllocationRuleModel, rule_id)
!         if model is not None:
!             await self.session.delete(model)
!             await self.session.flush()
  
  
> class BidOptimizationRuleRepository:
>     def __init__(self, session: AsyncSession):
!         self.session = session
  
>     async def save(self, rule: BidOptimizationRule) -> BidOptimizationRule:
!         model = BidOptimizationRuleModel.from_domain(rule)
!         merged = await self.session.merge(model)
!         await self.session.flush()
!         return merged.to_domain()
  
>     async def find_by_id(self, rule_id: UUID) -> BidOptimizationRule | None:
!         result = await self.session.execute(
!             select(BidOptimizationRuleModel).where(BidOptimizationRuleModel.id == rule_id)
!         )
!         model = result.scalar_one_or_none()
!         return model.to_domain() if model is not None else None
  
>     async def find_by_organization(self, org_id: UUID) -> list[BidOptimizationRule]:
!         result = await self.session.execute(
!             select(BidOptimizationRuleModel)
!             .where(BidOptimizationRuleModel.organization_id == org_id)
!             .order_by(BidOptimizationRuleModel.name)
!         )
!         return [m.to_domain() for m in result.scalars().all()]
  
>     async def delete(self, rule_id: UUID) -> None:
!         model = await self.session.get(BidOptimizationRuleModel, rule_id)
!         if model is not None:
!             await self.session.delete(model)
!             await self.session.flush()
  
  
> class AudienceSegmentRepository:
>     def __init__(self, session: AsyncSession):
!         self.session = session
  
>     async def save(self, segment: AudienceSegment) -> AudienceSegment:
!         model = AudienceSegmentModel.from_domain(segment)
!         merged = await self.session.merge(model)
!         await self.session.flush()
!         return merged.to_domain()
  
>     async def find_by_id(self, segment_id: UUID) -> AudienceSegment | None:
!         result = await self.session.execute(
!             select(AudienceSegmentModel).where(AudienceSegmentModel.id == segment_id)
!         )
!         model = result.scalar_one_or_none()
!         return model.to_domain() if model is not None else None
  
>     async def find_by_organization(self, org_id: UUID) -> list[AudienceSegment]:
!         result = await self.session.execute(
!             select(AudienceSegmentModel)
!             .where(AudienceSegmentModel.organization_id == org_id)
!             .order_by(AudienceSegmentModel.name)
!         )
!         return [m.to_domain() for m in result.scalars().all()]
  
>     async def delete(self, segment_id: UUID) -> None:
!         model = await self.session.get(AudienceSegmentModel, segment_id)
!         if model is not None:
!             await self.session.delete(model)
!             await self.session.flush()
  
  
> class ContentRecommendationRepository:
>     def __init__(self, session: AsyncSession):
!         self.session = session
  
>     async def save(self, rec: ContentRecommendation) -> ContentRecommendation:
!         model = ContentRecommendationModel.from_domain(rec)
!         merged = await self.session.merge(model)
!         await self.session.flush()
!         return merged.to_domain()
  
>     async def find_by_id(self, rec_id: UUID) -> ContentRecommendation | None:
!         result = await self.session.execute(
!             select(ContentRecommendationModel).where(ContentRecommendationModel.id == rec_id)
!         )
!         model = result.scalar_one_or_none()
!         return model.to_domain() if model is not None else None
  
>     async def find_by_organization(self, org_id: UUID,
>                                     rec_type: str | None = None) -> list[ContentRecommendation]:
!         query = select(ContentRecommendationModel).where(
!             ContentRecommendationModel.organization_id == org_id
!         )
!         if rec_type:
!             query = query.where(ContentRecommendationModel.recommendation_type == rec_type)
!         query = query.order_by(ContentRecommendationModel.confidence_score.desc())
!         result = await self.session.execute(query)
!         return [m.to_domain() for m in result.scalars().all()]
  
>     async def delete(self, rec_id: UUID) -> None:
!         model = await self.session.get(ContentRecommendationModel, rec_id)
!         if model is not None:
!             await self.session.delete(model)
!             await self.session.flush()
  
  
> class AutomationRuleRepository:
>     def __init__(self, session: AsyncSession):
!         self.session = session
  
>     async def save(self, rule: AutomationRule) -> AutomationRule:
!         model = AutomationRuleModel.from_domain(rule)
!         merged = await self.session.merge(model)
!         await self.session.flush()
!         return merged.to_domain()
  
>     async def find_by_id(self, rule_id: UUID) -> AutomationRule | None:
!         result = await self.session.execute(
!             select(AutomationRuleModel).where(AutomationRuleModel.id == rule_id)
!         )
!         model = result.scalar_one_or_none()
!         return model.to_domain() if model is not None else None
  
>     async def find_by_organization(self, org_id: UUID) -> list[AutomationRule]:
!         result = await self.session.execute(
!             select(AutomationRuleModel)
!             .where(AutomationRuleModel.organization_id == org_id)
!             .order_by(AutomationRuleModel.name)
!         )
!         return [m.to_domain() for m in result.scalars().all()]
  
>     async def find_enabled(self, org_id: UUID) -> list[AutomationRule]:
!         result = await self.session.execute(
!             select(AutomationRuleModel)
!             .where(
!                 AutomationRuleModel.organization_id == org_id,
!                 AutomationRuleModel.enabled == True,
!             )
!             .order_by(AutomationRuleModel.name)
!         )
!         return [m.to_domain() for m in result.scalars().all()]
  
>     async def delete(self, rule_id: UUID) -> None:
!         model = await self.session.get(AutomationRuleModel, rule_id)
!         if model is not None:
!             await self.session.delete(model)
!             await self.session.flush()
