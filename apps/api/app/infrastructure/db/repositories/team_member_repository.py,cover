> from uuid import UUID
  
> from sqlalchemy import select
> from sqlalchemy.ext.asyncio import AsyncSession
  
> from app.application.ports.repositories import TeamMemberRepository
> from app.domain.entities.team_member import TeamMember
> from app.infrastructure.db.models.team_member import TeamMemberModel
  
  
> class TeamMemberRepositoryImpl(TeamMemberRepository):
>     def __init__(self, session: AsyncSession):
!         self.session = session
  
>     async def save(self, member: TeamMember) -> TeamMember:
!         model = TeamMemberModel.from_domain(member)
!         merged = await self.session.merge(model)
!         await self.session.flush()
!         return merged.to_domain()
  
>     async def find_by_id(self, member_id: UUID) -> TeamMember | None:
!         result = await self.session.execute(
!             select(TeamMemberModel).where(TeamMemberModel.id == member_id)
!         )
!         model = result.scalar_one_or_none()
!         return model.to_domain() if model is not None else None
  
>     async def find_by_organization(self, org_id: UUID) -> list[TeamMember]:
!         result = await self.session.execute(
!             select(TeamMemberModel).where(TeamMemberModel.organization_id == org_id)
!         )
!         models = result.scalars().all()
!         return [m.to_domain() for m in models]
  
>     async def find_by_user_and_organization(self, user_id: UUID, org_id: UUID) -> TeamMember | None:
!         result = await self.session.execute(
!             select(TeamMemberModel).where(
!                 TeamMemberModel.user_id == user_id,
!                 TeamMemberModel.organization_id == org_id,
!             )
!         )
!         model = result.scalar_one_or_none()
!         return model.to_domain() if model is not None else None
  
>     async def find_by_user(self, user_id: UUID) -> list[TeamMember]:
!         result = await self.session.execute(
!             select(TeamMemberModel).where(TeamMemberModel.user_id == user_id)
!         )
!         return [m.to_domain() for m in result.scalars().all()]
  
>     async def delete(self, member_id: UUID) -> None:
!         result = await self.session.execute(
!             select(TeamMemberModel).where(TeamMemberModel.id == member_id)
!         )
!         model = result.scalar_one_or_none()
!         if model is not None:
!             await self.session.delete(model)
!             await self.session.flush()
