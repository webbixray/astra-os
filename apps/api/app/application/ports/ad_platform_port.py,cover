> from abc import ABC, abstractmethod
  
> from app.domain.entities.adplatforms.platform import AdCampaign, AdPlatform
  
  
> class AdPlatformPort(ABC):
-     @abstractmethod
-     async def get_campaigns(self, account_id: str) -> list[AdCampaign]:
-         ...
  
-     @abstractmethod
-     async def get_performance(
-         self, account_id: str, campaign_ids: list[str] | None = None
-     ) -> list[AdCampaign]:
-         ...
  
-     @abstractmethod
-     async def create_campaign(self, campaign: AdCampaign) -> str:
-         ...
  
-     @abstractmethod
-     async def update_campaign_status(
-         self, campaign_id: str, status: str
-     ) -> bool:
-         ...
  
-     @property
-     @abstractmethod
-     def platform(self) -> AdPlatform:
-         ...
