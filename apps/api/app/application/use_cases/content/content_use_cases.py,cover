> from uuid import UUID
  
> from app.domain.entities.content.content import Content
> from app.domain.exceptions.domain_exceptions import EntityNotFoundError
  
  
> class ContentRepository:
-     async def save(self, content: Content) -> Content:
-         ...
  
-     async def find_by_id(self, content_id: UUID) -> Content | None:
-         ...
  
-     async def find_by_organization(self, org_id: UUID, status: str | None = None) -> list[Content]:
-         ...
  
-     async def find_by_campaign(self, campaign_id: UUID) -> list[Content]:
-         ...
  
-     async def delete(self, content_id: UUID) -> None:
-         ...
  
  
> class CreateContentUseCase:
>     def __init__(self, repo: ContentRepository):
!         self.repo = repo
  
>     async def execute(
>         self,
>         organization_id: UUID,
>         title: str,
>         content_type: str,
>         created_by: UUID,
>         campaign_id: UUID | None = None,
>         body: str = "",
>         brand_profile_id: UUID | None = None,
>         generated_by_ai: bool = False,
>     ) -> Content:
!         content = Content.create(
!             organization_id=organization_id,
!             title=title,
!             content_type=content_type,
!             created_by=created_by,
!             campaign_id=campaign_id,
!             body=body,
!             brand_profile_id=brand_profile_id,
!             generated_by_ai=generated_by_ai,
!         )
!         return await self.repo.save(content)
  
  
> class GetContentUseCase:
>     def __init__(self, repo: ContentRepository):
!         self.repo = repo
  
>     async def execute(self, content_id: UUID) -> Content:
!         content = await self.repo.find_by_id(content_id)
!         if content is None:
!             raise EntityNotFoundError("Content", str(content_id))
!         return content
  
  
> class ListContentUseCase:
>     def __init__(self, repo: ContentRepository):
!         self.repo = repo
  
>     async def execute(self, org_id: UUID, status: str | None = None) -> list[Content]:
!         return await self.repo.find_by_organization(org_id, status=status)
  
  
> class UpdateContentUseCase:
>     def __init__(self, repo: ContentRepository):
!         self.repo = repo
  
>     async def execute(
>         self,
>         content_id: UUID,
>         title: str | None = None,
>         body: str | None = None,
>         status: str | None = None,
>     ) -> Content:
!         content = await self.repo.find_by_id(content_id)
!         if content is None:
!             raise EntityNotFoundError("Content", str(content_id))
!         if title is not None:
!             content.title = title
!         if body is not None:
!             content.update_body(body)
!         if status is not None:
!             status_methods = {
!                 "review": content.submit_for_review,
!                 "approved": content.approve,
!                 "published": content.publish,
!             }
!             method = status_methods.get(status)
!             if method:
!                 method()
!             else:
!                 content.status = status
!         content.updated_at = __import__("datetime").now()
!         return await self.repo.save(content)
